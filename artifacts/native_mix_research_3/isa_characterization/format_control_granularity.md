# Format-control granularity

Result: `INCONCLUSIVE`.

The old report describes an `m16n8k64` instruction footprint and software dispatch granules, but
the Research 3 adjacent-group hardware probe was not run. No minimum format-control scope is
claimed. The one-sided K2 type block must be derived from a future probe, not assumed to be 16×64.

Blocking issue: SM120 GPU unavailable.
