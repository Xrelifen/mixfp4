# Logs

Only read-only audit output and CPU reference-test output belong here. There are no GPU job logs:
zero GPU subprocesses were launched. Prior aggregate performance claims had no raw logs and remain
`UNVERIFIED`.
